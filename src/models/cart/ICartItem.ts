export interface ICartItem {
    category: string,
    created_at: string,
    description: string,
    id: number,
    image: string,
    name: string,
    price: number,
    stock: number,
    quantity: number
  }
